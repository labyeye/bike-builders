<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>Edit Bike</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      padding: 2rem;
    }
    .form-container {
      background: white;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="form-container">
      <h3 class="mb-4">Edit Bike</h3>
      <form action="/admin/bike/edit/<%= bike._id %>" method="POST">
        <div class="row">
          <div class="col-md-6 mb-3">
            <label class="form-label">Brand*</label>
            <input type="text" name="brand" value="<%= bike.brand %>" class="form-control" required />
          </div>
          <div class="col-md-6 mb-3">
            <label class="form-label">Model*</label>
            <input type="text" name="model" value="<%= bike.model %>" class="form-control" required />
          </div>
        </div>

        <div class="row">
          <div class="col-md-6 mb-3">
            <label class="form-label">Model Year*</label>
            <input type="number" name="modelYear" value="<%= bike.modelYear %>" class="form-control" min="2000" max="2024" required />
          </div>
          <div class="col-md-6 mb-3">
            <label class="form-label">KM Driven*</label>
            <input type="number" name="kmDriven" value="<%= bike.kmDriven %>" class="form-control" min="0" required />
          </div>
        </div>

        <div class="row">
          <div class="col-md-6 mb-3">
            <label class="form-label">Ownership*</label>
            <select name="ownership" class="form-select" required>
              <option value="1st Owner" <%= bike.ownership === '1st Owner' ? 'selected' : '' %>>1st Owner</option>
              <option value="2nd Owner" <%= bike.ownership === '2nd Owner' ? 'selected' : '' %>>2nd Owner</option>
              <option value="3rd Owner" <%= bike.ownership === '3rd Owner' ? 'selected' : '' %>>3rd Owner</option>
              <option value="4th Owner or more" <%= bike.ownership === '4th Owner or more' ? 'selected' : '' %>>4th Owner or more</option>
            </select>
          </div>
          <div class="col-md-6 mb-3">
            <label class="form-label">Fuel Type*</label>
            <select name="fuelType" class="form-select" required>
              <option value="Petrol" <%= bike.fuelType === 'Petrol' ? 'selected' : '' %>>Petrol</option>
              <option value="Diesel" <%= bike.fuelType === 'Diesel' ? 'selected' : '' %>>Diesel</option>
              <option value="EV" <%= bike.fuelType === 'EV' ? 'selected' : '' %>>Electric</option>
            </select>
          </div>
        </div>

        <div class="row">
          <div class="col-md-6 mb-3">
            <label class="form-label">Days Old*</label>
            <input type="number" name="daysOld" value="<%= bike.daysOld %>" class="form-control" min="0" required />
          </div>
          <div class="col-md-6 mb-3">
            <label class="form-label">Price (₹)*</label>
            <input type="number" name="price" value="<%= bike.price %>" class="form-control" min="0" required />
          </div>
        </div>

        <div class="row">
          <div class="col-md-6 mb-3">
            <label class="form-label">Downpayment (₹)*</label>
            <input type="number" name="downPayment" value="<%= bike.downPayment %>" class="form-control" min="0" required />
          </div>
          <div class="col-md-6 mb-3">
            <label class="form-label">Image URL</label>
            <input type="text" name="imageUrl" value="<%= bike.imageUrl %>" class="form-control" placeholder="https://example.com/bike-image.jpg" />
          </div>
        </div>

        <div class="mb-3">
          <label class="form-label">Status*</label>
          <select name="status" class="form-select" required>
            <option value="Available" <%= bike.status === 'Available' ? 'selected' : '' %>>Available</option>
            <option value="Coming Soon" <%= bike.status === 'Coming Soon' ? 'selected' : '' %>>Coming Soon</option>
            <option value="Sold Out" <%= bike.status === 'Sold Out' ? 'selected' : '' %>>Sold Out</option>
          </select>
        </div>

        <div class="d-flex justify-content-between">
          <a href="/admin/dashboard" class="btn btn-secondary">Cancel</a>
          <button type="submit" class="btn btn-primary">Save Changes</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>